# Example configuration file for GitHub Actions workflow
# Copy this file to veracode-devcount-github.yml and update with your settings
# Or place it in the repository root as veracode-devcount-github.yml

global-network:
  # Optional: SSL Certificate Configuration
  # ssl:
  #   reject-unauthorized: true
  #   ca-file: "/path/to/ca-certificate.pem"
  
  # Optional: Proxy Configuration
  # proxy:
  #   host: "proxy.company.com"
  #   port: 8080
  #   protocol: "http"
  #   auth:
  #     username: "proxy-user"
  #     password: "proxy-pass"
  
  # Optional: Rate Limiting Configuration
  rate-limit:
    requests-per-hour: 4000
    requests-per-minute: 60
    delay-between-requests: 1000
    max-retries: 5
    backoff-multiplier: 2

dev-count:
  - ci-system: "github"
    # Token will be overridden by GITHUB_TOKEN environment variable if set
    # You can set a placeholder here or leave it empty
    ci-token: "placeholder-will-be-overridden"
    domain: "https://api.github.com"
    # Optional: regex pattern for email filtering
    # regex: "/github\\.com$/i"
    # Optional: regex file path
    # regex-file: "/path/to/regex/file"

